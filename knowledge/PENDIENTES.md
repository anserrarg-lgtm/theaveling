# Pendientes activos — Design System Figma

> Lista de trabajo viva. Se actualiza cada vez que se resuelve o surge algo nuevo. No borrar items resueltos — marcarlos como hechos, para tener historial.

Última actualización: sesión del 2026-08-22/24.

---

## Abierto — necesita decisión o prompt

> **Verificación directa en Figma de la tanda de 14 prompts (sesión de hoy):** 13 de 14 corrieron perfecto — SANSITA, jerarquía/espaciado de detalle-mobile (itemSpacing 64 confirmado en `detail-content`, 5 títulos a 24px, párrafo intro a 70%), borde de error, nota de búsqueda vacía, íconos viejos + gap del buscador (8px confirmado), Ghost Loading + focus ring (4 anillos confirmados), ancho de Reservation Card (350px + divisor FILL confirmados), búsqueda en verde completa (mobile+desktop confirmados), buscador verde (caja + ícono/texto, todo confirmado en el maestro), Moneda en Preferencias, Notificaciones/Datos de cuenta (Switch + 2 pantallas + flecha confirmados), y Card System ronda 2 (Featured borrado, panel de layout, panel responsive, intro — los 4 confirmados). El único con problema real es el de las advertencias en rojo — ver ítem propio abajo.

- **Nombres viejos "SANSITA" en Typography — prompt ya mandado.** Confirmado: 3 frames en "01.2 — Typography" tienen nombre técnico con "SANSITA" aunque el texto visible ya dice "ARCHIVO". Prompt: `prompt_limpiar_nombres_sansita.md`.

- **Jerarquía visual + espaciado de detalle-mobile — prompt ya mandado (reemplaza el anterior en pausa).** Se sumó un hallazgo nuevo: el espacio entre las 5 secciones es 32px, pero la escala documentada para separar secciones dice 64–96px (32px es el rango de "separación entre componentes", no de secciones). Prompt combinado: `prompt_jerarquia_y_espaciado_detalle_mobile.md` — sube itemSpacing a 64px, sube los 5 títulos a H2 24px, baja opacidad del párrafo introductorio a 70%.

- **Rename "Piece Info Hero" (nombre técnico en Figma).** Ana no ve motivo para cambiarlo — queda descartado, no se toca.

- **Borde rojo de error en Input — decidido, prompt ya mandado.** Ana confirmó: el texto rojo solo alcanza (no hace falta borde), así que se borra la línea de documentación que lo prometía. Prompt: `prompt_borde_error.md`.

- **Nota de "resultados vacíos sin error" — decidido, prompt ya mandado.** Se saca de Input Specs y se suma a "Búsqueda — Definido", que es donde realmente pertenece. Prompt: `prompt_nota_busqueda_vacia.md`.

- **Íconos viejos + espacio del buscador — prompt ya mandado.** Símbolos de texto viejos (⌕ ▾ 📅) en 6 lugares de documentación, reemplazados por el nombre real del ícono. Además, el espacio entre el ícono de lupa y el texto del buscador es de 1px real (debería ser 8px) por una alineación mal configurada. Prompt: `prompt_iconos_viejos_y_espacio_buscador.md`.

- **Loading del botón Icon — decidido, prompt ya mandado.** Revisé cómo está construido Loading en Primary/Secondary/Ghost antes de armar esto: ninguno tiene spinner real, los 3 solo cambian el texto a una palabra de carga y bajan la opacidad (pero ni siquiera con el mismo número: 70/60/100%). Como Icon no tiene texto, no se puede copiar ese truco — se aplica la misma idea (bajar opacidad) al ícono, con el valor de Secondary (60%), por ser el botón más parecido en peso visual. Prompt: `prompt_icon_button_loading.md`.

- **Ghost Loading + anillo de foco — prompt ya mandado.** Corrige 3 cosas: agrega Loading a Ghost (mismo patrón que Secondary), agrega el anillo de foco que le faltaba por completo a las 4 variantes "Focused" de Input (bug real, no tenían ningún efecto), y corrige 5 textos que documentaban un valor de anillo de foco equivocado (2px+2px) — el valor real ya construido en los botones es 2px blanco + 4px verde Thea al 45%, que coincide con lo que ya decía "01.5 — Icons". Prompt: `prompt_ghost_loading_y_focus_ring.md`.

- **Cards: sobre-especificación en el catálogo — decidido, prompt ya mandado.** Las 3 "Experience Card — Ver más" (Más reservados/Descubrimientos/Curado) eran 3 componentes separados y duplicados aunque la documentación ya las trataba como 1 solo tipo — se fusionan en 1 componente real con variante "Sección". Prompt: `prompt_fusionar_ver_mas.md`. Correrlo antes que la reescritura de Card System (ítem de abajo), porque esa reescritura reorganiza esos mismos nodos.

- **Card System: reescritura grande — decidido (2 preguntas), prompt ya mandado.** Era el trabajo más grande pendiente. Se confirmó con números reales: la anatomía/imagen/espaciado de "Experience Card" describía una card genérica (320px, imagen ~11:9 al 64% de altura) que no coincide con ninguna de las 5 variantes reales construidas. También se confirmó que "Curated/Featured Card" (versión horizontal para desktop) nunca se construyó — solo existe un boceto viejo dibujado a mano, mientras que en mobile el rail de Curado ya usa la card vertical real. Decisiones de Ana: Card System sigue documentando todo el universo de cards (agrupado en descubrimiento vs. flujo), y Curated/Featured Card se deja marcada como "pendiente de construir" en vez de borrarse. El prompt agrupa las 5 variantes de Experience Card en 1 sola entrada, separa descubrimiento de flujo, reescribe anatomía/imagen/espaciado con los números reales, marca Featured como pendiente en todos los lugares donde aparece, y saca el número viejo "las otras 5" de Booking Summary Card. Reemplaza por completo al prompt anterior de "fantasma + dato viejo" (ya borrado). Prompt: `prompt_card_system_reescritura.md`.

- **Patterns: Booking / Seat Map / Compra se leían como 3 cosas sueltas — decidido, prompt ya mandado.** Confirmado que son 3 etapas del mismo journey (Seat Map ya decía "se usa dentro del flujo de compra", Booking Summary Card ya estaba documentada como parte del flujo de Compra). Se reordenan (Compra → Seat Map → Booking), se renombran como "Pattern / Compra — 1/2/3", se reescribe la intro genérica de la sección (hoy describe solo a Booking) y se le agrega a Booking su propia descripción, que le faltaba. Prompt: `prompt_journey_compra_patterns.md`.

- **Card System, ronda 2 (5 puntos de Ana) — revisado uno a uno, prompt ya mandado.** (1) Featured/Curated se borra por completo — reemplaza la decisión anterior de dejarla "pendiente de construir". Al borrarla no queda ninguna card horizontal real en el sistema, así que el panel "Vertical vs Horizontal" se reescribe (ya no hay nada que comparar). (2) Contradicción responsive confirmada como real: el catálogo dice que Experience Card vive en "rail de Home" pero el panel de mobile decía que esa misma familia usa una grilla que colapsa de 3 columnas a 1 — se corrige para que diga rail horizontal, igual que Supporting. (3) Featured vs. Curated se resuelve solo, al borrar Featured. (4) Familia/componente/variante: se precisa que solo Experience Card es una familia real (5 variantes) — los otros 6 son componentes independientes sin variantes, nunca deberían llamarse "familia". (5) Conteos viejos: revisé toda la sección y el resto del archivo, "11 componentes / 7 familias" es el único texto de conteo que existe y ya es correcto — no encontré ninguna referencia vieja escondida. El punto de Patterns (unificar Booking/Compra) ya estaba resuelto de antes, sin acción nueva. Prompt: `prompt_card_system_ronda2.md`.
- **Íconos de Input fuera de la escala de "01.5 — Icons" — decidido, prompt ya mandado.** Medí los 3 íconos reales (no solo el texto): la lupa de Search mide 18px real (la escala solo permite 16/20/24, así que pasa a 20px — Medium está documentado para "inputs"), la flecha de Select mide 14px real (pasa a 16px, Small). El de Date en realidad ya mide 16px real y está bien — la documentación decía "14px" por error, eso sí era solo un dato viejo. Se corrigen los 2 íconos reales y los 6 textos que los mencionan (en "01.5 — Icons" y en "Input Specs"). Prompt: `prompt_iconos_input_fuera_de_escala.md`.
- **08 — Perfil: Notificaciones y Datos de cuenta, definidos — prompt ya mandado.** Ana decidió: Notificaciones es pantalla propia con lista de tipos (cada uno con switch — se crea el componente "Switch" nuevo, no existía ninguno en el archivo, usando solo colores/efectos ya reales: Thea Green sólido, Thea Green 12% del borde de Booking Selector Card, sombra "Theaveling/Shadow/Small"). Datos de cuenta es pantalla propia con Nombre, Correo, Teléfono (Input Type=Text real), Cerrar sesión y Eliminar cuenta (Button Ghost real, con texto en Thea Red para diferenciar la acción destructiva — no existe un botón "danger" en el sistema, se resolvió así en vez de inventar una variante nueva de Button). La fila de Notificaciones en Preferencias pasa de mostrar "Activadas" a la misma flecha "→" que ya usa Datos de cuenta. Prompt: `prompt_notificaciones_y_datos_cuenta.md`.
- **08 — Perfil: Moneda ya no está abierta, era nota vieja — prompt ya mandado.** ARCHITECTURE.md ya tenía el alcance de Moneda resuelto desde el 18 de agosto (moneda de pago preferida, autodetectada por país, editable, con USD como alternativa), pero la nota del canvas nunca se actualizó. Se corrige la nota. Notificaciones y Datos de cuenta sí siguen genuinamente sin definir — no hay decisión tomada sobre si son un switch inline o llevan a pantalla propia; queda marcado como pendiente real, no resuelto todavía. Prompt: `prompt_preferencias_moneda_resuelta.md`.
- **05 — Búsqueda: cerrado.** Solo muestra Sugerencias, no depende de que Filtros esté terminado. No es un pendiente.
- **Bug real: Reservation Card "Status=Próxima" mide 557px dentro de pantalla de 390px — prompt ya mandado.** El componente maestro quedó mal dimensionado (ni el contenido ni la línea divisoria interna llenan ese ancho). Se lleva a 350px, igual que "Status=Pasada" (que sí está bien). Prompt: `prompt_arreglar_ancho_reservation_card_proxima.md`.
- **4 textos de advertencia usaban naranja fuera de paleta, y el de Tendencias quedó confuso — prompt ya mandado.** Ana lo notó al ver el prompt anterior ya corrido. Los 4 pasan a Thea Red, y el texto de Tendencias se reescribe en 2 frases claras (falta el criterio de orden / los títulos son ejemplo). Prompt: `prompt_arreglar_naranja_advertencias.md`.
- **06 — Filtros: se borra entero — decidido, prompt ya mandado.** Ana decidió que como no hay ningún filtro específico definido, la sección no debería existir. Se borra completa, y en su lugar (en la pantalla de búsqueda mobile) va una sección nueva "Tendencias" (texto plano, sin miniatura, orden por puntuación de Descubrimientos marcado como pendiente de definir). También se construye el equivalente de escritorio, que no existía — dropdown real debajo del buscador del desktop-navbar, con Sugerencias (con miniatura) y Tendencias. Prompt: `prompt_tendencias_busqueda.md`. Correr después de `prompt_sugerencia_con_miniatura.md` (usa el componente Suggestion Item que crea ese prompt).
- **Sugerencias de Búsqueda con miniatura — prompt ya mandado.** Ana pidió que las sugerencias sean piezas con foto en miniatura, no solo ícono+texto. Se crea el componente real "Suggestion Item" (miniatura 40×40px + texto, fondo transparente, no verde — mismo criterio que Supporting Card) y se aplica a las 4 sugerencias reales. Prompt: `prompt_sugerencia_con_miniatura.md`.
- **"Featured" seguía mencionada en 9 lugares más del Card System, además de lo ya borrado — decidido, prompt ya mandado.** Ana pidió revisar si quedaba algo suelto y sí: Anatomía, Guía de Variantes, Sistema de Imagen, Espaciado (3 veces), Tipografía y Comportamiento Responsive (2 veces) todavía tenían filas dedicadas a Featured, más una mención en el párrafo de introducción general y 3 menciones sueltas dentro de frases sobre cards reales (heart icon, estado Pressed, estado Saved). Se borran las 9 filas completas y se corrigen las 6 menciones sueltas sin tocar el resto de cada frase. Prompt: `prompt_limpiar_featured_card_completo.md`.
- **Pantalla de confirmación (feedback después de "Confirmar reserva") — decidido, prompt ya mandado.** Ana no quiere una card ni un bottom-sheet: quiere ícono de check centrado en verde menta + texto de éxito/error + dónde encontrar el ticket. Ya existía el componente real "Type=Success" (con Type=Error/Warning/Info/Loading como hermanos) — no hubo que construir nada nuevo, solo corregirlo: el check usaba un verde escrito a mano (coincidía con "Semantic/Success", no con menta) en vez de la variable real "Interaction/Thea Mint", y el texto no mencionaba dónde encontrar el ticket. Prompt: `prompt_completar_pantalla_confirmacion.md`.
- **Buscador verde invisible en mobile, distinto al de desktop — decidido, prompt ya mandado.** Corregido con screenshots reales: el buscador de escritorio SÍ tenía un ajuste manual puesto directo sobre esa instancia (ícono blanco 100%, texto placeholder blanco 60%) que nunca se llevó al componente maestro. Mobile, al no tener ese ajuste, se quedó con el color por defecto del maestro (Thea Green oscuro, invisible sobre fondo verde) — por eso mobile y desktop no se veían igual. Se lleva ese mismo valor exacto de la instancia de escritorio al maestro, así queda consistente y no se puede desalinear de nuevo. Reemplaza a `prompt_arreglar_icono_texto_buscador_verde.md` (ya borrado, tenía valores equivocados — Off-white 0.984/50% en vez de blanco puro/60% real). Prompt: `prompt_igualar_buscador_verde_a_desktop.md`.
- **2 bugs reales encontrados y confirmados en Figma — prompt ya mandado.** (1) El componente Input "Type=Search, Surface=Green" tiene su caja interna con blanco sólido 100% en vez de Off-white al 8% (mismo error de variante nunca ajustada) — por eso el buscador del navbar verde se ve como un bloque blanco. (2) 4 textos nuevos de la reescritura de Card System quedaron con ancho 0, por lo que el texto se apila letra por letra en vertical. Prompt: `prompt_arreglar_input_verde_y_textos_card_system.md`.
- **Versión verde ("Fondo verde") de Búsqueda/Tendencias — decidido, prompt ya mandado.** Mismo patrón Light/Green que ya existe en Button, Input, navbar, top bar, bottom nav y tabs, aplicado ahora a la búsqueda. Se crea "Suggestion Item — Fondo verde" (texto Off-white), se duplica "search-screen" completo en verde (mobile) y se duplica la composición de escritorio completa en verde, sin tocar ninguna de las versiones claras originales. De paso corrige, dentro de las copias verdes, el color de las advertencias (naranja → Thea Red) y el texto de la advertencia de Tendencias. Reemplaza por completo a `prompt_corregir_navbar_verde_busqueda.md` (ya borrado — ese modificaba la composición original en vez de crear una copia en paralelo, rompiendo la convención del archivo). Prompt: `prompt_busqueda_fondo_verde.md`.

---

## Resuelto y verificado esta sesión

- Botón "Guardar" (Secondary) sobre fondo verde — creado y confirmado, instancia real en Usage Examples.
- Corazón de Curado protegido — degradado nuevo arriba (120px), confirmado.
- Reservation Card — texto de prueba ya no aparece (ocultado y vaciado), ficha de catálogo nueva agregada. Confirmado con screenshot.
- "Pattern / Compra" — creado, confirmado con chequeo directo en Figma (instancias reales de Booking Selector Card + Información adicional + botón Siguiente).
- Descripción huérfana de "Featured + Rail" en Patterns — borrada, confirmado.
- Etiqueta vieja "PENDIENTE — Sin definición de producto" en la nota de Búsqueda — borrada, frame renombrado a "Búsqueda — Definido", confirmado.
- Buscador sobre fondo verde (Input `Surface=Green`) — creado y aplicado en el navbar real de escritorio, confirmado que ahora se ve bien. "Usage Examples" reconstruido con instancias reales (Input y botón Primary), confirmado.
- "Pattern / Seat Map" movido — ya no está adentro de "Pattern / Booking", es su propio patrón, confirmado.
- Card "Curated / Featured Card" huérfana — ya no existe en el archivo (se ve que se resolvió en algún momento), no queda ninguna instancia suelta. Solo quedan menciones en documentación (Card Types, Specs, etc.), que son legítimas.
- Texto blanco del catálogo en cards "Ver más" (segunda ubicación, "Card Components") — corregido a verde Thea 40%.
- 3 cards "Ver más" con fotos distintas entre sí.
- Card del espacio dentro de "Información adicional" ahora usa Artist/Space Card real.
- Ícono decorativo sin función quitado de "Tipo de entrada".
- `icon/x` creado; reemplaza el X dibujado a mano en "Información adicional".
- "Pattern / Seat Map" creado en 03 — Patterns.
- Restructuración completa de Compra: `compra-mobile` es frame de ejemplo (no card formal); "Booking Selector Card" es la card real (fecha+hora, tipo de entrada, asientos — sin información adicional), fondo Off-white 100% sólido + borde 1px Thea Green 12% (decidido tras confirmar que la transparencia no se lee bien sobre fondo verde); "informacion-adicional-bottom-sheet" es frame simple con borde superior, no card formal.
- Pantalla `detalle-mobile` construida completa (6 secciones + Piece Info Hero con foto real).
- Rail "Contenido similar" corregido: usa Supporting Card (no Experience Card, que rompía el ancho mobile).
- Supporting Card: nueva variante "Size=Compact" (220px) con botón de me gusta, usada en el rail de Contenido similar. Variante Default (280px) intacta para el patrón de escritorio.
- Botones "Reservar" (Detalle) y "Siguiente" (Compra) — nueva variante real del Button `Surface=Green` (fondo Off-white, texto verde) para que se vean sobre pantallas de fondo Thea Green sólido; incluye estado Disabled propio.
- Corazón de Experience Card (Curado, Contenido completo, Más reservados, Descubrimientos) — ya no se corta contra el borde de su marco (`clipsContent: false`).
- Alturas de Descubrimientos parejas: 340×512 en las 5 cards reales + su Ver más (antes: 512/507 mezclados, y el Ver más en 492).
- Descubrimientos formalizado como componente propio "Experience Card — Descubrimientos" (independiente del componente compartido con Más reservados), documentado en catálogo y en Card Family → Card Types.
- Punto 10 (documentar comportamiento de cards en desktop) — ya estaba hecho, confirmado en el catálogo (texto "Mobile no es un desktop reducido...").
- Booking Review Card ("Evaluar y continuar") descartada — Ana confirmó que no hace falta. Lo que sí existe y está completo/verificado es Booking Summary Card (categoría, título, fecha+hora, ubicación, personas, total, botón real "Confirmar reserva").
- Las 4 advertencias en rojo — confirmado que ahora usan la variable real "Brand/Thea Red" (`VariableID:1386:4`), no un valor suelto. Verificado directo en Figma.
- Tanda de 14 prompts de la sesión de hoy — 13/14 confirmados perfectos, el único con problema (rojo mal vinculado) ya se corrigió y se reverificó, arriba.
- Las 2 instancias sueltas del wordmark "Theaveling" — se quedan como están, no se tocan. Decisión de Ana.

---

## Lista numerada de Ana (puntos 3–11, verificados 2026-08-24)

- **Punto 3 (Piece Info Hero, título sobre imagen + bloque inferior verde):** CONFIRMADO, corrió bien. Título en blanco sobre la imagen, bloque inferior (Precio/Descripción) en verde Thea con texto blanco. Sin superposición con los chips.
- **Punto 4 (card "Ver Todo" angosta, título+artista):** resuelto sin construir nada nuevo — "Experience Card — Contenido completo de sección" (320×200) ya tiene título+artista (no título+descripción). Es la misma card del punto 11, unificado.
- **Punto 5 y 6 (card "+N", estándar + especial Curado):** YA EXISTEN — es el mismo componente que "Experience Card — Ver más", una por categoría (Más reservados, Descubrimientos, Curado), con foto oscurecida + conteo + "Ver más". No hace falta construir nada. Solo están sin usar en una pantalla real todavía porque no existe ninguna pantalla de tipo Home/rieles construida en el proyecto (Ana confirmó: no es un problema, es que esa pantalla no se ha hecho).
- **Punto 7 (Reservation Card como bottom-sheet blanco, sale al tocar Comprar):** no arrancada.
- **Punto 10 (documentar comportamiento desktop):** ya estaba hecho (ver arriba), se deja para el final de todas formas.
- **Punto 11 (separar Curado de Contenido completo de sección):** CERRADO — confirmado. Curado separado en su propio componente, rail mobile usa las instancias correctas, y el conteo de Card Family ya dice "doce tipos" (confirmado con chequeo directo en Figma).

---

- **Pattern / Compra — bloque de Información adicional roto — decidido, prompt ya mandado.** Ana cuestionó si Patterns tiene sentido en general; se aclaró que sí, porque Compra y Descubrimiento van a repetirse en más experiencias de escena y cultura (no es una copia inútil de una sola pantalla). Pero ESTE Pattern específico tenía 2 bugs reales: el bloque "Información adicional" estaba en blanco sin ninguna base (ni siquiera existe metido en la pantalla real compra-mobile, solo vive como maqueta suelta acá) — pasa a verde Thea real, con todos los textos e íconos invertidos a Off-white (mismo patrón Light/Green de siempre). Y el botón "Siguiente" quedaba flotando solo arriba con ~1000px de espacio vacío abajo (por quedar estirado a la altura del bloque de información, que es mucho más alto) — se centra verticalmente con `layoutAlign`, sin tocar la posición de los otros 2 elementos. Prompt: `prompt_arreglar_pattern_compra_seleccion.md`.
- **Nota para más adelante: separar Patterns de Experience Card por variante.** Ana sugirió que, a diferencia de Compra/Descubrimiento (que se van a repetir tal cual), Experience Card podría necesitar un Pattern separado por cada variante (Más reservados, Curado, etc.) porque cada una es distinta. No es urgente, queda anotado para cuando se llegue a ese punto.
- **Botón "Siguiente" del Pattern/Compra — el color ya quedó bien, el centrado del botón sigue sin confirmar.** El prompt de seguimiento (`prompt_centrar_boton_siguiente_pattern.md`) se mandó pero no se verificó todavía si corrió. Probablemente quede reemplazado por el punto de abajo (reconstruir el Pattern completo como maqueta vertical), en vez de arreglarse suelto.
- **Prompt de las flechas de flujo — RETRACTADO, no correr.** `prompt_flecha_flujo_pattern_compra.md` insertaba flechas `icon/caret-right` entre las 3 piezas del Pattern/Compra, tratándolas como pasos secuenciales. Ana aclaró que el bloque de Información adicional en realidad va apilado en la misma pantalla, debajo del selector de fecha y hora — no es un paso siguiente. El diagrama que se armó con este mismo modelo equivocado (`diagrama_pattern_compra_flujo.html`) también quedó mal y no debe usarse como referencia.
- **Pattern / Compra — 1. Selección: pendiente reconstruir como maqueta vertical de pantalla (390px), no como fila horizontal con flechas.** Selector arriba, bloque de información apilado debajo, botón "Siguiente" fijo abajo — así es como realmente va en la pantalla real. Propuesto, sin construir todavía; quedó englobado dentro del pedido más amplio de "aproximaciones de pantallas y flujo".
- **Aproximación de Home (rieles) — primer intento tenía un bug real, ya corregido en un 2do prompt sin verificar todavía.** El primer prompt (`prompt_aproximacion_home_mobile.md`) usó por error el componente "Experience Card — Ver más" (la card oscura de "+8") en las 3 secciones, en vez de las cards de contenido reales — confirmado con screenshot, las 3 secciones mostraban la misma card gris repetida. De paso se confirmó que "Más reservados" nunca tuvo su componente real construido (solo texto de documentación) y que faltaban las Category Tabs (Todo/Escena/Cultura) en el header. Prompt de arreglo: `prompt_arreglar_home_mobile_y_card_mas_reservados.md` — construye el componente real "Experience Card — Más reservados" (clonado de Descubrimientos, 340×540, imagen más grande), repone "home-header" que había quedado suelto fuera de "home-mobile", suma las Category Tabs (fondo verde, Active=Todo), y reconstruye home-content con las 3 cards correctas en orden Curado → Más reservados → Descubrimientos (Curado primero, como pidió Ana). Sin verificar todavía. Una vez confirmada la estructura, sigue: afinar contenido real de las cards (fotos distintas — ahora Más reservados y Descubrimientos comparten la misma foto de placeholder), y después construir el Pattern de flujo/journey que conecte Home → Detalle → Compra → Confirmación.

- **Segundo color de la paleta: "Brand/Thea Green Deep" — corregido el orden, prompt mandado (reemplaza al anterior).** Ana pidió que primero se agregue como variable real en la paleta (como Thea Green/Red/Mint) antes de aplicarlo a nada — el primer intento (`prompt_verde_oscuro_category_tabs.md`) ponía el número suelto directo en el componente, sin pasar por la paleta; quedó retractado, Ana no llegó a pegarlo. Prompt correcto: `prompt_agregar_verde_profundo_paleta.md` — crea la variable, le agrega swatch en la documentación de Marca, y recién ahí vincula las Category Tabs a esa variable (95% opacidad). El header ("mobile-top-bar — Fondo verde") sigue en el verde plano — no se tocó, pendiente decidir si también debería llevar el oscuro o se queda igual al fondo a propósito.
- **Nav de Home cambiada a la versión verde translúcida (coincide con el frame de referencia) — prompt mandado.** Home usaba la nav clara (Off-white) parada sobre el fondo verde; se reemplaza por la instancia real de "Mobile Bottom Navigation — Fondo verde". Prompt: `prompt_cambiar_nav_home_a_verde.md`.
- **Stroke superior de la nav inferior: existía una 2da versión (verde) con su propio stroke, sin arreglar — prompt mandado.** El primer arreglo solo tocó la nav clara (Off-white). Hay un componente maestro totalmente aparte, "Mobile Bottom Navigation — Fondo verde" (usado en el frame EJEMPLO HOME THEA), con su propio borde superior (blanco 20%) en los 3 estados — nunca se había tocado. Prompt: `prompt_quitar_stroke_nav_inferior_verde.md`. Pendiente de confirmar con Ana: Home hoy usa la nav CLARA (Off-white) parada sobre el fondo verde — el frame de referencia en cambio usa la nav VERDE translúcida. Preguntar si hay que cambiar la de Home a la verde para que coincida.
- **Header de Home: se confirma usar el logo+lupa ("mobile-top-bar"), no la caja de buscador — prompt mandado.** Aclarado de paso: el Input "Type=Search" SÍ se usa en otros lados reales (navbar de escritorio, la propia pantalla de búsqueda) — no queda huérfano, solo no correspondía en Home. "mobile-top-bar" ya era un componente real (usado en Reservas y Perfil), solo le faltaba versión verde — se crea "mobile-top-bar — Fondo verde" y se usa en Home. Prompt: `prompt_componentizar_header_busqueda_verde.md`.
- **Calendario para "Por fecha" — sí existía, Ana tenía razón — prompt mandado.** Estaba dibujado como boceto suelto ("Date Selector": mes + flechas + días + una semana con un día seleccionado) dentro de Pattern / Compra — 3. Confirmación → Selectors, nunca se había convertido en componente. Se componentiza y se agrega a las 2 pantallas de búsqueda justo debajo de "Por fecha". Sin cambios de color entre versiones — ya usa el mismo tratamiento real de Booking Selector Card (Off-white + borde verde sutil) que funciona igual sobre fondo claro y verde. Aviso: tal como está, solo muestra 1 semana, no un mes completo — se dejó así (fiel al boceto), pendiente confirmar con Ana si hace falta expandirlo a mes completo. Prompt: `prompt_componentizar_date_selector.md`.
- **Fila "Por fecha" agregada arriba de Tendencias en la pantalla de búsqueda (clara + verde) — prompt mandado, calendario real todavía sin definir.** Mismo patrón que "Notificaciones"/"Datos de cuenta" en Perfil (texto + flecha). El calendario que se abriría al tocar esa fila no existe todavía en el sistema (no hay ningún componente de calendario real, solo el ícono) — no se inventó, queda como pendiente de definición de producto con Ana antes de construirlo. Prompt: `prompt_agregar_por_fecha_busqueda.md`.
- **Decidido, sin construir todavía (actualizado): Curado en Home va a ser 3 cards apiladas en scroll vertical, no un riel horizontal.** Corrección del punto anterior (que decía "1 sola card") — Ana aclaró que son 3, en scroll vertical (una debajo de la otra, no lado a lado). Debajo de la 3ra card, a la derecha, va un texto subrayado tipo "+8" para seguir viendo más (en vez de la card oscura "Ver más" que se usaba antes para esto). Como consecuencia, Curado deja de necesitar la card de "Ver más" — se borra esa variante del sistema (prompt ya mandado: `prompt_borrar_ver_mas_curado.md`, confirmado que no la usa ninguna instancia). Falta armar el layout nuevo (3 cards en columna + texto "+8" al final) cuando Ana lo pida — anotado acá para que quede registrado, no se construye todavía. Ya se verificó el estilo exacto real del "+8" en "EJEMPLO HOME THEA continuacion del scroll": texto "+8" (Archivo Regular 24px, Off-white 100%, subrayado) + ícono `icon/caret-right` (24×24) al lado, en fila horizontal con 14px de espacio entre los 2, alineados a la derecha justo debajo de la card, con ~16px de separación respecto a la card de arriba.
- **Descubrimientos rediseñada como card horizontal compacta (300×240) — decidido, prompt mandado.** Ana confirmó la opción de simplificar y pidió que además se vea claramente distinta a las otras 2. Queda: imagen apaisada + título (18px) + lugar/fecha, sin categoría/descripción/valoración/precio (no entraban en 240px de alto). Mantiene fondo Off-white 6% y esquinas redondeadas 12px (a diferencia de Curado, que quedó plana) — así las 3 cards del sistema quedan visualmente distintas entre sí. Documentación actualizada en el mismo prompt. Prompt: `prompt_descubrimientos_rectangular.md`.
- **El resize de Más reservados a 268×368 no se había aplicado — confirmado directo en Figma, reintentado suelto.** El componente y sus 3 instancias en Home seguían en 340×540 (el prompt original lo traía junto con otras cosas y no corrió esa parte, mismo patrón que ya pasó una vez con el `layoutAlign` del Pattern de Compra). Prompt de reintento, solo: `prompt_reintentar_tamano_mas_reservados.md`.
- **Ajuste de tamaños de cards en Home + estética general — prompt parcial mandado, 1 pregunta abierta.** Ana pidió: Curado sin bordes, Más reservados a 268×368, Descubrimientos a 300×240, y señaló el frame real "EJEMPLO HOME THEA" (+ su continuación) como referencia de estética para las pantallas. Encontrado: el "borde" de Curado es el corner radius de 12px del contenedor de imagen (no hay stroke) — se baja a 0. Más reservados sí entra en 268×368 (contenedor de imagen pasa a `layoutSizingVertical: FILL` para no dejar un número fijo que se rompa si el título pasa a 2 líneas). Descubrimientos NO entra en 300×240 con su anatomía completa (categoría+título+descripción+metadata+valoración+precio mide ~152–190px solo de texto, casi no deja espacio para imagen) — se le propuso a Ana simplificarla a imagen+título superpuesto (como Curado), pendiente su confirmación antes de construir esa parte. Prompt (solo Curado + Más reservados + textos de documentación actualizados): `prompt_ajustar_tamanos_cards_home_v2.md`. Además: el frame de referencia real muestra un header con logo "Theaveling" + ícono de búsqueda (no la caja de buscador que se usó) y Curado como 1 sola card grande a todo el ancho (no riel de 3) — señalado como diferencia grande, no tocado todavía, a la espera de que Ana confirme si quiere que Home adopte esa estética completa.

- **Curado en Home (3 cards + "+8"): Ana confirmó que NO se arma todavía.** Se iba a construir apenas quedara documentado el estilo exacto del "+8" (ver entrada de arriba), pero Ana aclaró que ese layout se va a usar una sola vez en todo el archivo — no vale la pena construirlo antes de que haga falta de verdad. Queda solo registrado (estilo exacto ya documentado arriba), sin construir, hasta que lo pida explícitamente.

- **Auditoría completa: blanco puro (#FFFFFF) usado donde debería ir Off-white (#FBFBFB) — decidido, prompt mandado.** Ana lo señaló como un problema recurrente ya avisado antes. Barrido de las 168 coincidencias de blanco puro en todo el archivo: la gran mayoría son copias automáticas de 6 focos reales (arreglar el maestro arregla todas sus instancias). Los 6 focos reales: header claro "mobile-top-bar" (fondo), ícono de lupa + texto placeholder del buscador verde (componente compartido "Type=Search, Surface=Green" — se usa en header verde, navbar de escritorio verde y pantallas de búsqueda), texto de "desktop-navbar — Fondo verde", Category Tabs verde (3 estados: texto + underline) y Mobile Bottom Navigation verde (3 estados: íconos + texto + indicador activo). Todos se vinculan a la variable real "Light/Off-White" (no un número suelto), manteniendo la opacidad exacta que ya tenía cada nodo. Dejados afuera por ser blanco legítimo: insignias "Favorito Boton" e íconos de Piece Info Hero (escala White/N documentada para insignias sobre foto), los swatches que documentan esa misma escala, y un rótulo de sección en la documentación. Prompt: `prompt_arreglar_blanco_a_offwhite.md`.
  - **Caso límite 1 — check ✓ de selección de asientos (Pattern/Compra, `1848:681`): CERRADO, decisión de Ana — se queda en blanco puro, no se toca.** Se revisó a fondo antes de cerrarlo: por un lado, la escala "Blanco sobre Thea Green" de STACK.md (que dice evitar blanco puro) técnicamente aplica — el check está parado sobre un fill sólido de Thea Green, no sobre una foto como las insignias que sí quedaron exceptuadas. Por otro lado, surgió una tercera opción (mint/"Interaction Accent", que el propio sistema documenta para "checkmarks de selección", con precedente real en el check de "Pago exitoso"). Ana decidió no complicarlo más: se queda como está, en blanco puro. Sin prompt, no hace falta ninguna acción en Figma.
  - **Caso límite 2 — fill translúcido blanco ~8% del botón Secondary/Ghost/Icon sobre verde (`1650:369`): decidido, prompt mandado.** Confirmado con el prompt original que armó el set (`prompt_buttons_fondo_verde.md`): el ~8% sale de la misma escala "Blanco sobre Thea Green" de STACK.md, ya usada correctamente (vinculada a Off-white) en el botón "Guardar" verde y en la caja del buscador verde — mismo criterio, mismo número, solo faltaba vincular el color. Pasa a la variable real "Light/Off-White", manteniendo la opacidad exacta de cada uno de los 12 nodos (6 Secondary + 1 Ghost/Hover + 5 Icon). No incluye el color de texto/ícono de estos botones (no auditado, queda fuera de alcance a propósito — pendiente de revisar en algún momento si hace falta). Prompt: `prompt_offwhite_secondary_ghost_icon_verde.md`.

- **Botón con el verde oscuro nuevo, pensado para inicio de sesión — decidido, prompt mandado.** No existe ninguna pantalla de login en el archivo todavía (revisado, no aparece por ningún lado) — este prompt solo crea el botón suelto, listo para cuando se construya esa pantalla. Es un componente nuevo "Button — Fondo verde oscuro" (clonado de Style=Primary de "Button — Fondo verde"), fondo vinculado a la variable real "Brand/Thea Green Deep" al 95% (mismo criterio que tabs/nav), texto Off-white, contenido "Iniciar sesión". Solo el estado Default por ahora — si se construye la pantalla real, faltaría sumar Hover/Pressed/Focus/Disabled/Loading. Prompt: `prompt_boton_verde_oscuro_login.md`.

- **Category Tabs de home-mobile: 2 bugs reales encontrados y corregidos — prompt mandado.** (1) El maestro real (clara Y verde, las 2 revisadas) usa alineación SPACE_BETWEEN en vez de CENTER — por eso "Todo" queda pegado al borde izquierdo y "Cultura" al derecho en vez de verse agrupadas al centro. No es solo de home-mobile, es del componente — se corrige en las 2 versiones. (2) Específicamente en home-mobile, tanto el header como las Category Tabs quedaron estirados en alto respecto a su tamaño real (header 79px en vez de 56, tabs 80px en vez de 40) — se corrige el alto de los 2, y se recorren hacia arriba todos los elementos de abajo (título, cards, nav inferior) la misma cantidad, más el alto total de la pantalla, para que no quede ni hueco vacío ni todo apretado. Prompt: `prompt_arreglar_category_tabs_centrado.md`.

- **Botón "Iniciar sesión" quedó angosto (136px, hug) — confirmado y corregido, prompt mandado.** Se lleva a 350px fijo (mismo criterio real que ya usa Reservation Card: "390px de pantalla − 20px cada lado"), texto centrado. Prompt: `prompt_ancho_boton_login.md`.

- **Reservation Card en el catálogo de Card System — decidido (Ana: "sí, sumala"), prompt mandado.** Revisando más a fondo: en realidad SÍ tenía una "Ficha completa" propia y ya estaba contada en el texto de introducción ("11 componentes reales") — mi primer diagnóstico no fue del todo preciso ahí. Lo que sí faltaba de verdad: no aparecía en ninguna de las 5 secciones transversales del sistema (Anatomía, Radio + Superficies, Estados, Acciones + Íconos, Variantes), y 2 de esas secciones ("Radio + Superficies", "Card States") hacían afirmaciones generales ("todos los tipos de tarjeta") que son falsas para Reservation Card (no tiene imagen ni cornerRadius/superficie propia). Se agrega su entrada en Anatomía, se corrigen las 2 afirmaciones falsas, se suma el estado de acordeón expandido/colapsado (nuevo, no documentado en ningún lado), se agregan 3 íconos reales que introdujo esta card y que faltaban en el catálogo (icon/ticket, icon/map, icon/caret-down), y se corrige la afirmación de "las 11 cards usan layout con imagen" en Variantes. Hallazgo aparte, sin tocar: Piece Info Hero, Booking Selector Card y Booking Summary Card TAMPOCO tienen entrada en Anatomía — queda anotado para más adelante. Prompt: `prompt_sumar_reservation_card_al_catalogo.md`.

- **Tamaño de mobile: resuelto con el patrón real del sistema (auto-layout + FILL) — prompt final mandado, reemplaza a los 2 anteriores.** El tamaño ya registrado en el sistema es 390px (confirmado con las 8 pantallas reales — búsqueda, reservas, perfil, notificaciones, datos de cuenta, compra, detalle, información adicional — mostradas a Ana con screenshots). Ana pidió que además quedara "flexible" — se investigó cómo están armadas de verdad esas 8 pantallas (`reservas-screen` como referencia): auto-layout vertical, `primaryAxisSizingMode: AUTO` (alto se ajusta solo), cada hijo en `layoutSizingHorizontal: FILL`. Se replica ese mismo patrón real en "home-mobile" y "Ejemplo descubrimiento" (ambos convertidos de posicionamiento absoluto a auto-layout, ancho fijo 390, todo en FILL) — así si el ancho de referencia cambia de nuevo, alcanza con cambiar el frame, no hay que reajustar cada elemento a mano. Reemplaza por completo a `prompt_conformar_ejemplo_descubrimiento_a_390.md` (no pegar) y a la PASO 2 de `prompt_arreglar_category_tabs_centrado.md` (esa PASO 2 no pegar; la PASO 1 de ese mismo prompt — centrado de tabs en el maestro — sigue siendo válida, independiente). Prompt: `prompt_home_y_ejemplo_flexibles_390.md`. 2 hallazgos marcados para Ana en la nota: un segundo "Experience Card — Curado" de 44px de alto en home-mobile sin identificar qué es, y un gap de 14px en "Ejemplo descubrimiento" que no se replica exacto con este sistema (se deja en 0).

- **Componentes sueltos fuera del sistema — alcance CORREGIDO (era más chico de lo reportado), prompt mandado.** Mi primer reporte tenía un error: dije que desktop-navbar, mobile-top-bar, Mobile Bottom Navigation y Category Tabs (clara y verde, las 8) estaban sueltos — falso, mi script de detección no reconocía "02.1 — Navigation" ni "02.2 — Category Tabs" como secciones válidas (son secciones propias, hermanas de "02 — Components"). Verificado con la ruta completa: las 8 YA están bien ubicadas. Lo que sí queda genuinamente suelto (confirmado con ruta completa vacía = hijos directos de la página): los 6 íconos maestros + la nota "Masters de componentes — no editar acá..." (que sí es legítima, solo le falta el frame contenedor), "Suggestion Item — Fondo verde", "Date Selector", el botón "Button — Fondo verde oscuro" (quedó suelto al crearlo, error mío), y la copia duplicada de "mobile-top-bar — Fondo verde" (`2154:965`, confirmado 0 usos en todo el archivo — se borra directo, no se mueve). Prompt: `prompt_ordenar_componentes_sueltos.md`.

- **Patterns: Ana confirmó que por ahora no se trabaja más ahí — queda pendiente, sin fecha.**

- **Las 5 pantallas de ejemplo (home-mobile, Ejemplo descubrimiento + las 3 "EJEMPLO HOME THEA...") a 390 y flexibles — CONSOLIDADO en 1 solo prompt final.** Los 2 prompts que estaban sueltos (`prompt_home_y_ejemplo_flexibles_390.md` y `prompt_3_ejemplos_scroll_a_390.md`) quedan reemplazados por uno solo, para no generar confusión de "cuál pego". Mismo contenido de fondo: home-mobile y Ejemplo descubrimiento pasan a auto-layout real (como las 8 pantallas reales); las otras 3 ("continuación del scroll" x2, "Ejemplo VER MAS") NO se convierten a auto-layout porque son capturas de un momento a mitad de scroll (cards cortadas, nav flotando) — se les corrige solo el ancho y las posiciones que dependían del alto viejo de las tabs. Re-verificado contra el estado actual real del archivo antes de mandarlo — funciona sin importar qué versión anterior se haya corrido. Prompt: `prompt_5_ejemplos_flexibles_390_FINAL.md`.

- **Verificación directa en Figma de los 5 prompts pendientes — TODOS confirmados corridos y correctos.** (1) Las 5 pantallas de ejemplo: confirmado, home-mobile 390×764 auto-layout VERTICAL, Ejemplo descubrimiento 390×860 auto-layout VERTICAL, los 3 "continuación del scroll"/"Ejemplo VER MAS" en 390 de ancho. (2) Blanco→Off-white: confirmado, mobile-top-bar claro + Category Tabs verde + Mobile Bottom Navigation verde todos con el fill vinculado a la variable real "Light/Off-White" (`VariableID:1386:6`). (3) Botón "Iniciar sesión": confirmado, 350px. (4) Reservation Card en el catálogo: confirmado, el contenido de las 5 entradas nuevas es correcto — pero los 5 frames clonados se quedaron con el nombre de capa del original (ej. dice "Artist / Space Card" en el panel de capas aunque el texto adentro ya dice "Reservation Card") — cosmético, no afecta el diseño. Prompt de limpieza: `prompt_renombrar_frames_clonados_card_system.md`. (5) Componentes sueltos: confirmado, los 6 íconos + nota están dentro de "01.5 — Icons → Icon Components", Date Selector y el botón de login dentro de "02 — Components", Suggestion Item — Fondo verde dentro de "05 — Búsqueda", y el duplicado de mobile-top-bar verde ya no existe (borrado).

- **2 hallazgos de ChatGPT sobre 01.5 — Icons, confirmados y resueltos — prompt mandado.** Ana le pasó a ChatGPT una revisión del Design System y le señalaron varios puntos; se cruzaron uno por uno contra lo que ya sabíamos. La mayoría ya estaban cerrados (Focus de botones, Responsive de Card System, Featured/Curado, "las otras 5" de Booking Summary, nomenclatura Pattern/Compra — los últimos 2 reverificados en vivo). Quedaron 2 reales, sin tocar:
  - Corazón en Unicode (♡→♥) en vez de nombrar `icon/heart`: encontrado en 2 lugares reales (`1480:55` "Active/Selected", `1480:83` "Patrones comunes" de Icon-Only Controls). Un tercer lugar similar (`1480:116`, "Current Icon Inventory") NO se toca — ahí el símbolo es intencional, mismo estilo que sus vecinos.
  - Contradicción de área táctil: no eran solo 2 fuentes como decía ChatGPT, sino 3 ("Size Scale" por tamaño de ícono, "Icon-Only Controls" con 44px fijo sin excepción, "Responsive Behavior" con 44 mobile/32 desktop) — las últimas 2 se contradecían directo. Ana decidió: regla única por dispositivo (44 mobile / 32 desktop, coincide con lo ya construido — el botón de favorito mide 32×32 real). Se corrige el texto de "Icon-Only Controls" para reflejar la excepción de desktop; "Size Scale" y "Responsive Behavior" no se tocan. Prompt: `prompt_corazon_unicode_y_touch_target.md`. **Corrido y verificado directo en Figma — los 4 cambios (2008:688/689... digo, 1480:55, 1480:83, 1480:77, y el 1480:116 sin tocar) están confirmados.**

- **Íconos de Input (14–18px genérico vs. valores reales) — CERRADO, verificado directo en Figma.** El prompt `prompt_iconos_input_fuera_de_escala.md` ya estaba corrido: "Input Specs" dice Search 20px, Select 16px, Date 16px — coincide con los valores reales. Sin acción pendiente.

- **Ratios de imagen por variante en Card System (Sistema de Imagen) — CERRADO, verificado directo en Figma con captura.** Confirmado como un problema real, más profundo de lo que parecía: la ficha "Experience Card (familia)" dentro de Sistema de Imagen seguía describiendo a Más reservados y Descubrimientos como una sola entrada, con los números viejos de antes de los resizes posteriores (308×272px dentro de una card de 340px, 512–540px de alto) — hoy miden 268×368 y 300×240 respectivamente, ya ni comparten forma (vertical vs. horizontal). El resumen general de las 5 variantes sí se había actualizado en su momento; esta ficha técnica específica quedó afuera de esa pasada y nunca se corrigió. La parte de "Curado / Contenido completo de sección" en la misma ficha SÍ seguía bien (no tenía números que pudieran quedar viejos) — se dejó sin tocar. Prompt: `prompt_separar_ratios_mas_reservados_descubrimientos.md`. Verificado: las 3 entradas quedaron separadas y correctas, los contenedores crecieron lo necesario, y la sección de Espaciado que viene después se corrió hacia abajo sin quedar nada superpuesto ni cortado.

**Con esto, los 9 puntos que ChatGPT señaló sobre el Design System quedaron todos resueltos** — 5 ya estaban cerrados de antes (reverificados en esta sesión), y los 4 restantes (corazón Unicode, área táctil, íconos de Input, ratios de imagen) se cerraron en esta misma sesión, cada uno verificado directo en Figma.

- **Segunda vuelta de ChatGPT sobre el Design System — 3 hallazgos reales, corrección de un error propio.** ChatGPT volvió a revisar y señaló 7 puntos. Se hizo un barrido de texto completo de toda la página (no nodo por nodo) para verificar cada uno en vivo. Resultado: 3 eran reales — y 2 de esos 3 los había dado por cerrados antes sin re-verificar el nodo exacto (confié en notas viejas de esta misma lista de pendientes en vez de volver a mirar Figma):
  - **Focus de botones:** "Button Specs" (`1470:44`) seguía con el valor viejo (2px+2px) — el fix anterior tocó otros 5 textos pero se salteó este.
  - **Responsive de Card System, Experience cards en mobile:** una fila puntual de la tabla (`1502:502`) seguía diciendo "apiladas, una por fila", contradiciendo al párrafo de la misma sección (`1502:521`) que sí dice rail horizontal — la ronda 2 corrigió el párrafo pero no esta fila.
  - **Referencia a "Pattern / Booking"** dentro de la Anatomía de Reservation Card (`1539:153`) — el frame del patrón sí se renombró hace rato, pero esta mención de prosa aparte nunca se actualizó.
  Prompt: `prompt_3_reales_focus_responsive_booking.md`. **Corrido y verificado directo en Figma — los 3 textos (1470:44, 1502:502, 1539:153) confirmados con el valor/contenido correcto.**
  
  Los otros 4 puntos de esa segunda vuelta NO eran reales, confirmados con evidencia directa: íconos de Input y área táctil de escritorio — ChatGPT los describió como si siguieran rotos, pero ya están corregidos y verificados con captura en esta misma sesión (posible que esté trabajando con información vieja/cacheada). Featured — cero menciones de la palabra en toda la página, no hay nada que desambiguar. Sistema de Imagen, Curado/Contenido completo agrupados — es un agrupamiento legítimo (comparten exactamente el mismo tratamiento de imagen: a sangre completa, sin padding), no un error como sí lo era el caso de Más reservados/Descubrimientos que si se corrigió.

---

- **Calendario "Date Selector" — CERRADO, corrido y confirmado por Ana.** Ver entrada de arriba (línea ~107): el prompt `prompt_componentizar_date_selector.md` se corrió y Ana confirmó el resultado. El componente real existe y está agregado debajo de "Por fecha" en las pantallas de búsqueda.

- **Regresión encontrada: botones Secondary/Ghost/Icon de "Button — Fondo verde" quedaron ilegibles (fondo blanco 100% sólido en vez de 8% translúcido) — prompt de arreglo mandado.** Ana reportó "hay un montón que solo salen en blanco porque tienen las letras en blanco" — confirmado con captura: los 6 Secondary se ven como rectángulos blancos vacíos y los 5 Icon como círculos blancos sin el corazón visible. Causa: el prompt que cerró el "caso límite 2" (`prompt_offwhite_secondary_ghost_icon_verde.md`) pedía vincular el fill a la variable Off-white **manteniendo la opacidad que cada nodo ya tenía** (~8%), pero lo que se aplicó fue la variable correcta al 100% de opacidad — perdió la transparencia. Con el texto/ícono ya blanco (fuera de alcance de ese prompt, sin tocar), blanco sobre blanco sólido = ilegible. Se confirmó el valor correcto (8%) contra el precedente ya verificado del botón "Guardar" verde (`bg-[rgba(251,251,251,0.08)]`) y contra la escala "Blanco sobre Thea Green" de STACK.md (8% = superficies sutiles). Prompt: `prompt_arreglar_opacidad_botones_verde.md` — solo baja la opacidad de los mismos 12 nodos a 8%, no toca color, texto ni Style=Primary. **CERRADO — corrido y verificado directo en Figma con captura: los 6 Secondary y los 5 Icon vuelven a verse translúcidos, con texto/ícono legible.**

- **Tokens de verde "1C4040" y "0A2121" — ENCONTRADOS, prompt de header/nav mandado; falta decidir qué hacer con los 2 colores del botón.** Ana tenía razón, mi primera búsqueda (por nombre de variable y como texto) no los iba a encontrar porque son colores sueltos puestos a mano en el fill, no variables ni texto. Revisando nodo por nodo cada estado de "Button": `#1c4040` es el fondo de "Style=Primary, Surface=Light, State=Hover" (`1470:2`) y `#0a2121` el de "Style=Primary, Surface=Light, State=Pressed" (`1388:4`) — ninguno vinculado a variable, ninguno igual a Green/100 (`#112c2c`) ni a Thea Green Deep (`#011414`). El resto del set de Button (Secondary/Ghost/Icon en Hover/Pressed, y todo "Button — Fondo verde") sí usa Thea Green con opacidad real, no hex inventados — el problema está aislado a esos 2 estados de Primary/Light. Ana confirmó por separado (no relacionado con el bug de los botones) que el verde oscuro ("Brand/Thea Green Deep") va también en el header con buscador y en la nav inferior, además de Category Tabs — prompt mandado y verificado: `prompt_verde_oscuro_header_y_nav.md` (vincula mobile-top-bar verde, desktop-navbar verde, y las 3 variantes de Mobile Bottom Navigation verde a esa variable al 95%, mismo criterio que Category Tabs). El Hover/Pressed de Button Primary/Light quedó visiblemente sin resolver — Ana lo siguió viendo como error en "02 — Components" y pidió que se resuelva ya en vez de seguir preguntando. Resuelto: Pressed (`1388:4`) pasa a "Brand/Thea Green Deep" (reusa la variable existente, mismo rol "más oscuro"); Hover (`1470:2`) pasa a "Green/100" con la opacidad de capa al 90% (mismo mecanismo de variación tonal que usa el resto del sistema, en vez de un hex suelto). Prompt: `prompt_arreglar_hover_pressed_button_primary_claro.md`. **CERRADO — corrido y verificado directo en Figma: Hover vinculado a Green/100, Pressed vinculado a Brand/Thea Green Deep, ningún color suelto.**

- **Reservation Card — el catálogo de Card System solo mostraba la variante "Pasada" — Ana confirmó agregar Próxima, prompt mandado.** El componente real tiene 2 variantes (Próxima: acordeones "Ver ticket"/"Ver en mapa"; Pasada: CTAs "Volver a reservar"/"Dejar reseña") y las dos están bien construidas y en uso real en la pantalla "07 — Reservas". Dentro de "04 — Card System" solo se veía Pasada (`1965:669`) — Próxima solo estaba en prosa. Prompt: `prompt_reservation_card_proxima_en_catalogo.md` — agrega el ejemplo visual de Próxima al lado de Pasada en "Card Components", corre "Experience Card — Ver más" para no superponer, y de paso borra la instancia suelta de Pasada con contenido placeholder (`2167:1847`) que quedó flotando sin pertenecer a ningún frame. **CERRADO — corrido y verificado directo en Figma: las 2 variantes se ven lado a lado, "Ver más" no se superpone, la instancia suelta ya no existe.**

- **Texto de ejemplo de Style=Primary: "Reservar experiencia" → "Reservar" — decidido, prompt mandado.** Ana prefirió la versión corta — coincide con lo que ARCHITECTURE.md ya documenta para el CTA real de la pantalla de Detalle ("CTA 'Reservar' fijo abajo"). Toca los 12 nodos de texto de los estados Default/Hover/Pressed/Focus/Disabled de Primary (claro y verde, más las 2 variantes Surface=Green sueltas en el frame de Button claro) — no toca Loading ("Reservando..."), ni Secondary/Ghost/Icon, ni el botón de login. Prompt: `prompt_texto_boton_primary_reservar.md`. **CERRADO — corrido y verificado directo en Figma: los 12 nodos de Primary (claro y verde) dicen "Reservar", Loading sigue "Reservando...", Secondary/Ghost/Icon sin cambios.**

Con esto, los 2 prompts que quedaban pendientes de correr ya están corridos y verificados — no queda ninguna acción abierta de esta sesión, salvo el housekeeping de abajo (no bloquea nada).

- **Tercera vuelta de ChatGPT sobre el Design System — 4 hallazgos reales, 2 descartados.** Esta vez ChatGPT dijo haber leído el árbol real de la página (no solo texto suelto) y señaló 6 puntos "🔴". Los 6 se chequearon en vivo, nodo por nodo:
  - **Reales (prompt mandado):**
    1. Input Specs (`1471:42`) sigue con la línea genérica "Íconos: 14–18px", vieja desde que se documentaron los valores reales por tipo (Search 20px, Select 16px, Date 16px) en el mismo bloque.
    2. Card System → "Actions + Icons" (`1502:443`) tiene su propia regla de área táctil ("44×44px sin excepción") que contradice la regla ya corregida en la segunda vuelta de ChatGPT (`1480:77`, 44 mobile/32 desktop) — esta zona quedó afuera de esa corrección.
    3. "Card Family" (`1502:126`) sigue diciendo que Reservation Card está "sin variantes" — quedó viejo desde que se agregó la variante Próxima al catálogo (ver entrada de arriba).
    4. "Experience Card — Ver más" (`1806:562`) sigue mencionando una 3ra variante para Curado (400×520px) que se borró del sistema hace rato (`prompt_borrar_ver_mas_curado.md`, ya confirmado sin uso) — hoy solo hay 2 variantes reales.
  - **No reales, descartados sin acción:** "unidad principal de descubrimiento" (texto general de uso de Experience Card) vs. rail horizontal de mobile — hablan de contextos distintos (uso general vs. comportamiento específico mobile), no se contradicen. "11 componentes" vs. claridad de Community Card — Community Card ya está descrita como no-componentizada donde corresponde; mejora cosmética menor, no error.
  Prompt: `prompt_tercera_vuelta_chatgpt.md`. Sin correr todavía.

- **Cuarta vuelta de ChatGPT sobre el Design System — 2 hallazgos reales, 1 descartado (no es bug, es mockup de referencia ya conocido).** ChatGPT esta vez señaló 3 puntos, ya con el árbol real leído:
  - **Reales (prompt mandado):**
    1. "Accessibility + Usability" dentro de Card System (`1502:537`) tiene su propia regla de área táctil ("44×44px sin excepción") — una 4ta zona que se quedó afuera cada vez que se corrigió esta regla en vueltas anteriores (Icons → Size Scale, Icons → Icon-Only Controls, Cards → Actions + Icons ya dicen "44 mobile/32 desktop").
    2. "Card Family" (`2008:686`) sigue diciendo que Más reservados mide 340×540px — número viejo de antes del resize. El componente real y "Sistema de Imagen" ya coinciden en 268×368px hace rato.
  - **Descartado, sin acción por ahora:** la instancia de Curado a 400px de ancho dentro de una pantalla de 390px ("EJEMPLO HOME THEA continuación del scroll", x=-5) es real, pero ese frame específico ya está marcado como "captura de un momento a mitad de scroll" (no se llevó a la grilla de 390px a propósito, mismo criterio de `prompt_5_ejemplos_flexibles_390_FINAL.md`). Además el layout final de Curado en Home todavía no está construido (decidido: 3 cards apiladas en vertical, no riel) — este mockup se va a reemplazar. No se documenta como "overflow intencional" porque no lo es; se deja anotado acá por si se quiere revisar cuando se retome Home.
  Prompt: `prompt_cuarta_vuelta_chatgpt.md`. Sin correr todavía.

- **Nueva funcionalidad: selección de ciudad/ubicación — decidido con Ana, 2 prompts mandados.** Ana pidió resolver cómo el usuario elige/confirma dónde está (2 referencias: Rappi y una app tipo Fever), con el requisito de que primero se pida ubicación aproximada para poder recomendar apenas se entra a Home. Investigué cómo lo hace Rappi antes de proponer nada (opera solo en ciudades/países específicos, GPS por defecto + fallback manual de dirección) y concluí que Theaveling necesita nivel **ciudad**, no dirección exacta (no reparte nada, solo cura contenido) — se adopta el estilo tipo "Lugar: Bogotá ⌄", no el de Rappi. Ana confirmó: control persistente en los 2 headers (mobile+desktop, claro+verde), Onboarding nuevo (no existía ninguno construido) que pide ubicación aproximada con fallback a selector manual, y **contenido curado real para 3 ciudades** (no un estado vacío): Bogotá + Madrid + Buenos Aires — confirmadas por Ana, reintroducidas porque ya habían sido ciudades de ejemplo reales antes del barrido de consistencia geográfica ("Madrid→Bogotá"). Ver ARCHITECTURE.md y DECISIONS.md, "Selección de ciudad/ubicación", para el detalle completo.
  - Prompt 1 (control + selector de ciudad): `prompt_selector_ciudad_control_y_picker.md` — crea "Location Selector" (claro y verde), lo agrega a los 4 headers reales, y arma "City Picker" (versión mobile bottom-sheet y desktop dropdown) con las 3 ciudades. Sin correr todavía.
  - Prompt 2 (Onboarding): `prompt_onboarding_ubicacion.md` — primera pantalla de Onboarding real del archivo, pide ubicación aproximada con 2 acciones (Permitir / Elegir manualmente), más el estado de selección manual que reusa el City Picker del prompt 1. Correr después del prompt 1. Sin correr todavía.
  - **Decidido con Ana (corrigiendo el alcance de arriba): solo Bogotá lleva contenido curado real.** Madrid y Buenos Aires se quedan en el selector como ciudades elegibles, pero sin Home propia armada — al elegirlas se ve un estado "Próximamente" en vez de contenido curado completo. Evita construir 2 Home completas para ciudades que no se pueden navegar de verdad (Design System estático, no prototipo interactivo funcional). No se necesita ningún prompt de contenido nuevo — pendiente sumar el estado "Próximamente" a los rows de Madrid/Buenos Aires en "City Picker Row" (chico, sin prompt propio todavía).
  - **Bug real encontrado tras correr el prompt 1 — prompt de arreglo mandado.** Ana avisó que el control quedó mal en los headers. Confirmado en Figma: (1) la versión verde quedó con el fondo "Light/Off-White" al 100% de opacidad en vez de 8% — mismo tipo de regresión que ya pasó una vez con los botones sobre verde — texto e íconos (también blancos) quedan invisibles encima. (2) la versión clara tiene el texto "Bogotá" con ancho fijo de 48px, más angosto que la palabra real, así que la flecha `icon/caret-down` queda superpuesta con la "á" en vez de separada por el gap. Prompt: `prompt_arreglar_location_selector.md`. Sin correr todavía.
  - **Revertido: el paso de Onboarding que bloqueaba (`prompt_onboarding_ubicacion.md`, ya corrido) se borra — Ana mostró cómo lo hace Airbnb realmente.** Captura real de Airbnb (buscador de "Experiencias"): nunca pide permiso al entrar, "Por la zona" es una fila más dentro de las sugerencias de destino, el permiso solo se dispara si se la elige a propósito. Prompt `prompt_revertir_onboarding_y_cerca_de_mi.md` — **superado por la versión final de abajo, no hace falta correrlo** (investigué en vivo y "Cerca de mí" no llegó a agregarse — si Ana lo pegó, no tuvo efecto; de cualquier forma ya no aplica).
  - **VERSIÓN FINAL, prompt mandado.** Nueva vuelta de conversación con Ana: confirmado que la detección de ciudad es automática por IP (sin permiso, técnica estándar, la usa la propia Airbnb) — no hace falta "Cerca de mí" ni ningún control en el header. El control real de cambiar de ciudad se muda a Búsqueda, como fila "Ubicación" agrupada con "Por fecha" (que de paso se saca de donde vivía mal metida, adentro del frame de Tendencias) en una card traslúcida con borde — no blanco sólido. El header pasa a tener solo un indicador chico sin acción (ícono + texto subrayado), en mobile debajo de Category Tabs y en desktop al lado del wordmark. Búsqueda se simplifica para no repetir Sugerencias/Tendencias como 2 secciones — mobile se queda con "Tendencias" (ahora con miniatura), desktop con "Sugerencias" — ambas suben de 4 a 5 ítems. **Esto deja obsoletos** `prompt_selector_ciudad_control_y_picker.md` (el pill del header, no el City Picker en sí, que se sigue usando), `prompt_arreglar_location_selector.md` (ya no aplica, el pill se borra entero) y `prompt_revertir_onboarding_y_cerca_de_mi.md` (ya no aplica). Prompt: `prompt_ubicacion_a_busqueda_y_header_liviano.md`. **Corrido — verificado en vivo: 4/5 pasos perfectos** (pill sacado de los 4 headers, Location Indicator creado y ubicado bien en mobile/desktop, estructura de Búsqueda mobile y desktop reorganizada correctamente). **3 correcciones reales, prompt consolidado mandado (reemplaza a un prompt intermedio ya descartado).** (1) las cards nuevas en los 2 contextos de fondo verde (Búsqueda mobile y dropdown de escritorio verde) quedaron invisibles — se armaron con "Green/8" (pensado para fondo claro) sobre un fondo que ya es ese mismo verde, matemáticamente el mismo color, cero contraste — se corrige a "Light/Off-White" 8%. (2) las 5 sugerencias de Tendencias en mobile usaron por error el "Suggestion Item" claro en vez de "Suggestion Item — Fondo verde" (texto invisible). (3) Ana pidió sacar el borde de las 6 cards (las 2 de mobile + las 2 de cada dropdown de escritorio) — se queda solo el relleno traslúcido, sin borde. De paso se agrega el ícono `icon/calendar` a "Por fecha" en los 3 contextos (le faltaba, a diferencia de "Ubicación" que ya tenía `icon/map-pin`). El dropdown de escritorio claro no tiene ningún problema de color, solo pierde el borde. Prompt: `prompt_arreglar_busqueda_final.md`. Sin correr todavía. Ver ARCHITECTURE.md/DECISIONS.md, "versión final", 2026-08-31.
  - **3 ajustes más, prompt mandado.** (1) "Por fecha" pierde el subrayado y la flecha "→", queda igual de simple que "Ubicación" (que nunca tuvo flecha). (2) Las cards de Tendencias/Sugerencias no tenían padding interno (a diferencia de la card de Ubicación+Por fecha, que sí) — las miniaturas quedaban pegadas al borde; se les suma 16px, mismo valor que la otra card. (3) En mobile, la barra de búsqueda quedaba con el doble de margen que las cards (40px vs 20px) por un padding duplicado en "Search Header" — se pareja a 20px. Prompt: `prompt_ajustes_por_fecha_y_margen_tendencias.md`. Sin correr todavía.
  - **Corregido el diagnóstico: el problema real era recorte de contenido, no color — prompt mandado (reemplaza al anterior sobre la X).** Ana avisó que el contenedor estaba cortando la barra de búsqueda. Confirmado: "Search Header" tiene `overflow-clip` activado y su contenido mide exactamente el mismo ancho que el frame (350px, sin margen) — cualquier variación mínima corta el borde derecho. Se saca el recorte. De paso, Ana pidió cambiar el ícono de cerrar (X) por un botón de volver (←), mismo patrón que ya usan Notificaciones/Datos de cuenta. Prompt: `prompt_arreglar_header_busqueda_mobile.md`. Sin correr todavía.
  - **Ana señaló el número correcto (6%, no el 8% que había inventado) — y de paso encontré la causa raíz de por qué la opacidad se rompe tan seguido en este archivo: el blanco nunca se tokenizó de verdad.** El verde sí está bien: "Green/8", "Green/20", "Green/50", "Green/100" son variables reales de Figma con la opacidad horneada adentro. El Off-white no tiene ningún equivalente — solo existe "Light/Off-White" sólido, y cada superficie translúcida sobre verde (botón "Guardar", buscador verde, header/nav, y ahora esto) se armó aplicando opacidad a mano sobre esa variable, nunca con un token real — de ahí que se pierda tan fácil (mismo patrón de bug repetido varias veces esta sesión). Prompt: `prompt_crear_escala_white_como_tokens.md` — crea la escala real `White/100` a `White/4` (mismo patrón que Green) y la usa en las 4 cards de Búsqueda + las 2 Experience Card reales (que hoy tampoco están vinculadas a variable, solo tienen el número puesto a mano). Sin correr todavía. **Pendiente aparte, no incluido en este prompt a propósito (alcance mucho más grande):** migrar retroactivamente el resto del archivo (botón verde, buscador verde, header/nav, etc.) de opacidad manual a estos tokens nuevos — sigue funcionando hoy, pero sin la protección de un token real. Evaluar si vale la pena hacerlo.
  - **Bug real: el botón de volver del buscador usaba un glyph de texto "←" en vez del ícono real del sistema — prompt mandado.** El ícono real para "volver" es `icon/caret-left`, ya construido y documentado explícitamente como el ícono reusado por Piece Info Hero. Se corrige. Sobre el corte de la barra de búsqueda que Ana volvió a reportar: revisado en vivo, el estado actual (después del prompt anterior) ya no tiene recorte activado y las medidas cierran exactas — puede ser una vista vieja, pendiente que Ana confirme después de este prompt si lo sigue viendo. Prompt: `prompt_icono_back_real_busqueda.md`. Sin correr todavía.

- **Pivot a código real (2026-08-31).** Ana pidió pasar de iteración en Figma a un proyecto de código de verdad, y aclaró que no hace falta ninguna herramienta separada llamada "Claude Code" — esta misma sesión ya tiene terminal, lectura/escritura de archivos e instalación de paquetes. Confirmado con ella (3 preguntas rápidas antes de construir, mismo criterio de "hablar antes de actuar" reforzado toda esta sesión): stack **React + TypeScript (Vite) + Tailwind CSS 4**, trabajar **directo en `C:\Users\anser\theaveling`** (su carpeta conectada), y empezar por el **flujo principal** (Descubrir → Detalle de experiencia → Compra → Confirmación) antes que Login/Onboarding/Perfil. **Limitación técnica real descubierta:** esta sesión no tiene una herramienta de terminal sobre la compu de Ana (sí puede leer/escribir archivos y listar carpetas vía el puente de dispositivo, pero no ejecutar `npm install`/`npm run dev` ahí) — por eso el proyecto se arma y prueba en el workspace de la nube (con terminal completa) y se sincroniza como archivos hacia `theaveling/app/` en su compu, en vez de construirse ahí directamente. Ana instala dependencias y corre `npm run dev` localmente una vez sincronizado.
  - Proyecto creado: `theaveling/app/` — Vite + React 19 + TypeScript, Tailwind 4 (config CSS-first en `src/index.css`, sin `tailwind.config.js`), React Router.
  - **Design tokens ya traducidos a código** desde STACK.md: colores de marca (`thea-green` `#112c2c`, `thea-red` `#f31006`, `thea-mint` `#2ecca6`, `off-white` `#fbfbfb`), la escala completa de transparencia "Blanco sobre Thea Green" (`white-100` a `white-4`, incluyendo el `white-6` confirmado el mismo día) y "Green sobre superficies claras" (`green-100` a `green-8`), y las 3 familias tipográficas (Archivo/Instrument Sans/Sansita, cargadas desde Google Fonts) — todo como tokens de Tailwind, ningún valor suelto en componentes (misma regla que se está aplicando en Figma con las variables `White/<N>`).
  - **4 pantallas scaffoldeadas como placeholders estructurales** (anatomía real de ARCHITECTURE.md, sin fidelidad visual pixel-perfect todavía): Descubrir (Mobile Top Bar + Category Tabs + Location Indicator + Sub-category Tabs + grid), Detalle de experiencia (Piece Info Hero + Por qué descubrir esto + Ficha + Artista + Comunidad + Contenido similar + CTA fijo), Compra (campos obligatorios + bloques plegables + CTA bloqueado hasta completar fecha/hora/asiento) y Confirmación. Se completan pantalla por pantalla, no todas de una vez.
  - Sincronizado a `theaveling/app/` en la compu de Ana vía el puente de archivos (`node_modules` no se sincroniza, Ana corre `npm install` localmente).
  - **Reorganización de carpetas (2026-08-31): `app/` eliminada, el código vive directo en `theaveling/`.** Ana comparó con la estructura de `orbit-app` (su otro proyecto, "beacon") y confirmó que ahí el código vive en la raíz de la carpeta, con `knowledge/` como hermana — no en una subcarpeta separada. Se empareja el mismo criterio: todos los archivos del proyecto (`src/`, `package.json`, `vite.config.ts`, etc.) se resincronizaron directo en `theaveling/`, y Ana borró la carpeta vieja `theaveling/app/` (incluyendo su `node_modules`) usando Claude Code local — ver `prompt_borrar_carpeta_app_vieja.md`. Confirmado sin pérdida de contenido (todo lo de `app/` ya estaba duplicado en la raíz antes de borrar).
  - **Cambio de flujo de trabajo para código (2026-08-31): de acá en más, el código se escribe con Claude Code corriendo local en la compu de Ana, no en esta sesión de Cowork.** Mismo patrón que ya se usaba con Figma: esta sesión investiga specs reales (acá vía Figma MCP, para código vía lectura directa de archivos/investigación), arma un prompt detallado, Ana lo pega en Claude Code parado en `theaveling/`, y esta sesión verifica el resultado después (captura o revisión de archivos). Motivo: Claude Code corre directo sobre los archivos reales de Ana, sin el paso de sincronización manual que sí hace falta con esta sesión (que corre en un espacio aparte, en la nube).
  - Home (Descubrir) reconstruida fiel a la pantalla real ya ensamblada en Figma (`home-mobile`, `2122:960`), con specs verificados en vivo (medidas, colores, tipografía) — no a partir de la descripción de ARCHITECTURE.md, que resultó desactualizada respecto a lo realmente construido en Figma. Estructura real: Mobile Top Bar → Category Tabs → fila de Ubicación → "Curado por Theaveling" (card full-bleed, sin bordes redondeados a pedido de Ana) → riel "Más reservados" → riel "Descubrimientos" → Bottom Navigation. Se agregaron íconos reales (aproximados, no el trazo exacto — la red del sandbox de esta sesión bloquea la descarga de SVG desde Figma, node IDs documentados en `icons.tsx` para reemplazar cuando se pueda exportar) y placeholders de imagen con ícono (en vez de rectángulo vacío) mientras no hay fotografía real.
  - **Gap real confirmado: Sub-category Tabs (nivel 2, ej. Recomendado/Más reservados/Nuevos descubrimientos) no existen construidas en Figma.** Buscado a fondo (toda la página, más específicamente dentro de "02.1 — Navigation" y "02.2 — Category Tabs" completas, hoja por hoja) — no están. Se había armado una versión inventada en código pero no llevaba a ningún lado (no filtraba nada), así que se sacó de Descubrir. El componente queda guardado en `SubCategoryTabs.tsx` para retomar si Ana define/encuentra el spec real.

## Housekeeping — CERRADO

- **Consistencia del conteo "Card Family → Card Types" — verificado directo en Figma, sin discrepancia.** El texto de introducción dice "11 componentes reales" (`1502:126`) y la fila "Card Types" (`1502:127`) efectivamente lista 1 familia de 5 variantes (Experience Card: Más reservados, Descubrimientos, Curado, Contenido completo de sección, Ver más) + 6 independientes (Supporting Card, Artist/Space Card, Piece Info Hero, Booking Selector Card, Booking Summary Card, Reservation Card) = 11. Agregar la variante Próxima a Reservation Card no suma un tipo nuevo (es una variante de uno ya contado), así que el conteo se mantiene correcto. Sin acción pendiente.
